# Article module

这是你的第一个业务模块，目前故意不提供实现。

建议你思考并逐步添加：

- 文章在 Go 中应该用什么结构体表示？
- `draft` 和 `published` 应该是布尔值还是状态？
- HTTP handler 是否应该直接执行 SQL？为什么？
- 列表接口应该返回完整正文吗？

不必一开始设计“完美分层”。先写出最小实现，遇到重复和测试困难后再重构。

